name: student_expense_tracker
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0-132.0.dev

dependencies:
  fl_chart: ^0.70.2
  flutter:
    sdk: flutter
  idb_shim: ^2.6.2
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
